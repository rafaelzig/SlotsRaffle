# SlotsRaffle
```
Usage: raffle.py [OPTIONS]

Options:
  --directory PATH         Path of the directory containing the input files - defaults to ./input
  --delimiter [comma|tab]  delimiter used in input and output files - defaults to tab
  --help                   Show this message and exit.
```